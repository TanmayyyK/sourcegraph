# ─────────────────────────────────────────────────────────────────────────────
# Vision Node — Environment Configuration
# Switch ORCHESTRATOR_URL here when the real Extractor is ready.
# NEVER commit secrets or real IPs to version control — add .env to .gitignore.
# ─────────────────────────────────────────────────────────────────────────────

# Target endpoint on the Master Orchestrator (loopback test points here)
ORCHESTRATOR_URL=http://100.69.253.89:8000/ingest/visual

# Vision Node server settings (consumed by uvicorn in main.py / Dockerfile)
VISION_HOST=0.0.0.0
VISION_PORT=8080

# Logging level: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO