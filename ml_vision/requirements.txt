# ─────────────────────────────────────────────────────────────────
# 1. API & Web Server (main.py, client.py)
# ─────────────────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.6  # Required by FastAPI for File() and Form() parsing
httpx>=0.24.0            # For async webhooks and client payload delivery
python-dotenv>=1.0.0     # To load the .env configurations

# ─────────────────────────────────────────────────────────────────
# 2. Vision Machine Learning (engine.py)
# ─────────────────────────────────────────────────────────────────
torch>=2.0.0             # PyTorch core
numpy>=1.24.0            # Array manipulation for YOLO bounding boxes
Pillow>=10.0.0           # Image processing and resizing (Image, ImageOps)
transformers>=4.30.0     # HuggingFace library for the CLIP ViT-B/32 model
ultralytics>=8.0.0       # Official library for YOLOv8n object detection

# ─────────────────────────────────────────────────────────────────
# 3. Audio Machine Learning (audio_engine.py)
# ─────────────────────────────────────────────────────────────────
faster-whisper>=1.0.0    # CTranslate2-backed Whisper for VRAM-efficient audio